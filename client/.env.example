# Frontend API configuration
VITE_BASE_URL=http://localhost:3000

# Optional fallback names supported by the app.
# Keep these empty unless your deployment uses one of these names instead.
VITE_BACKEND_URL=
VITE_API_URL=

# Price display
VITE_CURRENCY=$
